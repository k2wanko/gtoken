machine:
  timezone:
    Asia/Tokyo

dependencies:
  pre:
    - go get -t -d -v github.com/mitchellh/gox
    - go get -t -d -v github.com/tcnksm/ghr
    
deployment:
  release:
    branch: setup-ci
    commands:
      - gox --output "pkg//{{.Dir}}_{{.OS}}_{{.Arch}}"
      - cp -r pkg/ $CIRCLE_ARTIFACTS/
      - ghr --replace v0.0.1 -u $USERNAME -r $CIRCLE_PROJECT_REPONAME pkg